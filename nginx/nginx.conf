worker_processes 1;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;
events { worker_connections 1024; }
http {
  resolver 127.0.0.11 valid=30s ipv6=off;

  log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                  '$status $body_bytes_sent "$http_referer" '
                  '"$http_user_agent" "$http_x_forwarded_for"';

  access_log /var/log/nginx/access.log main;
  sendfile on;
  tcp_nopush on;
  keepalive_timeout 65;
  types_hash_max_size 2048;
  include /etc/nginx/mime.types;
  default_type application/octet-stream;

  upstream psp_frontend {
    zone psp_frontend 64k;
    server psp-frontend:80 resolve;
  }
  upstream bank_frontend {
    zone bank_frontend 64k;
    server bank-frontend:80 resolve;
  }
  upstream shop1_frontend {
    zone shop1_frontend 64k;
    server shop1-frontend:80 resolve;
  }
  upstream shop2_frontend {
    zone shop2_frontend 64k;
    server shop2-frontend:80 resolve;
  }
  upstream psp_api {
    zone psp_api 64k;
    server psp-api:80 resolve;
  }
  upstream bank_api {
    zone bank_api 64k;
    server bank-api:80 resolve;
  }
  upstream crypto_api {
    zone crypto_api 64k;
    server crypto-service:80 resolve;
  }
  upstream webshop_api {
    zone webshop_api 64k;
    server webshop-api:80 resolve;
  }

  server {
    listen 80;
    server_name psp.localhost;

    location /api/ {
      proxy_pass http://psp_api;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
      proxy_pass http://psp_frontend;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }

  server {
    listen 80;
    server_name bank.localhost;

    location /api/ {
      proxy_pass http://bank_api;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
      proxy_pass http://bank_frontend;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }

  server {
    listen 80;
    server_name shop1.localhost;

    location /api/ {
      proxy_pass http://webshop_api;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
      proxy_pass http://shop1_frontend;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }

  server {
    listen 80;
    server_name shop2.localhost;

    location /api/ {
      proxy_pass http://webshop_api;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
      proxy_pass http://shop2_frontend;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }
}
