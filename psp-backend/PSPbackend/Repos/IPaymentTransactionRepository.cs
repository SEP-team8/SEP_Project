using PSPbackend.Models;

namespace PSPbackend.Repos
{
    public interface IPaymentTransactionRepository
    {
        //Task CreateAsync(PaymentTransaction tx, CancellationToken ct);
        //Task<PaymentTransaction?> GetByBankPaymentRequestIdAsync(Guid paymentRequestId, CancellationToken ct);
        //Task UpdateAsync(PaymentTransaction tx, CancellationToken ct);
    }
}
